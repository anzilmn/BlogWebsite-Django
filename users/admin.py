 # from django.contrib import admin
 # vfrom .models import CustomUser

#already was in comment Register CustomUser model (optional, only if needed)
 # @admin.register(CustomUser)
 # class CustomUserAdmin(admin.ModelAdmin):
  #    list_display = ("id", "username", "email")  # Fields to show in admin panel
   #   search_fields = ("username", "email")  # Enable search
